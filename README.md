# Food Delivery API (Node + Express + PostgreSQL/Prisma)

## 🚀 Setup

1. **Create PostgreSQL DB**: `food_delivery`
2. **Copy `.env.example` → `.env`**  
   Set your `DATABASE_URL` + `JWT_SECRET`.
3. **Install dependencies**
   ```bash
   npm i
